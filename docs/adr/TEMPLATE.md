<!--
如何使用本模板（本注释块与下方「如何使用」说明均为模板专用，复制为新 ADR 后可整体删除）：
1. 复制本文件为 `00NN-title.md`：编号顺延现有 ADR（当前最大为 0005，下一个新决策即 0006、0007……），文件名小写为英文连字符。
2. 填好顶部「状态（Status）」「日期」「决策人」三项元信息。
3. 写满「背景（Context）/ 决策（Decision）/ 后果（Consequences）」三节；后果须含「正面 / 负面·取舍 / 后续注意事项」三段。
4. 在 `README.md` 顶部索引表补一行（编号、标题、一句话摘要）。
5. 提交流程：在 `chore/adr` 类分支提交（如 `chore/adr-0006-xxx`），由主理人统一合并推送，**不要直接改 `main`**。
-->

> **如何使用（模板专用，复制为新 ADR 后可删除本行说明）：** 复制本文件为 `00NN-title.md`（编号顺延，见 `README.md` 索引表），填好顶部状态与「背景 / 决策 / 后果」三节，并在 README 索引表补一行，最后提交到 `chore/adr` 类分支。

# ADR-00NN: <决策标题>

- 状态（Status）：Proposed（提议中）　<!-- 取值之一：Proposed（提议中）/ Accepted（已落地）/ Deprecated（已弃用）-->
- 日期：YYYY-MM-DD
- 决策人：<决策人，例如：高见远（架构师）/ 主理人齐活林>

## 背景（Context）

<!-- 描述面临的问题、约束、已有决策或外部条件。回答「为什么现在要做这个决定」。可用列点呈现关键痛点。 -->

## 决策（Decision）

<!-- 描述「决定了什么」。给出具体方案、关键代码 / 配置片段（如适用），以及涉及的文件与模块路径。 -->

## 后果（Consequences）

**正面：**

<!-- 该决策带来的收益、解决的问题。 -->

**负面 / 取舍：**

<!-- 付出的代价、引入的限制、刻意做的妥协。 -->

**后续注意事项：**

<!-- 落地后的约束、禁区、需同步维护的关联常量 / 配置，以及触发重新评估该决策的条件。 -->
