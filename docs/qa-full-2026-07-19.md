# QA 全量测试报告：LLM API Gateway（shimiaocheng-llm）

> 测试人：严过关（QA 工程师）　|　测试日期：2026-07-19　|　被测版本：当前 `master`（含 PR #16/#17/#20 等）
> 工作流：**仅测试**（不修改源码、不部署、不重启、不写生产库、不 git push）

---

## 0. 工作边界遵守声明（铁律已遵守）

- ✅ 未修改任何生产源码（仅阅读 + 运行既有测试 + 新增 1 个服务器只读脚本 `/tmp/qacheck.py`、`/tmp` 临时日志，**均不提交**）。
- ✅ 未部署 / 未重启服务（`systemctl` 仅 `is-active` 读状态）。
- ✅ 未对生产库做任何写操作（DB 仅 `SELECT` / `PRAGMA`）。
- ✅ 未执行 `git` 提交 / push / `gh`。
- ✅ 单测工具链使用 `/opt/homebrew/bin/go`（go1.26.5），**未使用 `make`**（Makefile 写死坏掉的 `/tmp/go`）。
- 所有「通过」结论均附执行证据（单测用例名 / curl 输出片段 / SSH 查询结果）；受限项已明确标注替代验证方式。

---

## 1. 功能梳理摘要（基于源码核对，非照抄地图）

### 1.1 页面

| 页面 | 路由 | 源码 | 预期行为 / 输入-输出规范 |
|---|---|---|---|
| **P1 用户查询页** | `/user/` | `web/user/index.html` | 用户输入 PIN（子 Key）→ `GET /v1/quota` 拉配额 → 渲染仪表盘。关键函数：`renderCountRow`（调用次数配额，`limit=0` 渲染「配额无效」防除零）、Token 总量行（`limit=0`=无限，内联于 `renderDashboard`）、`renderExpiryRow`（空串=永久、已过期红、7 天内橙、其余正常）。 |
| **P2 后台登录页** | `/m-7xa2/login` | `web/admin/login.html` | 管理员账号密码登录，签发 `admin_session` cookie；未登录访问 `/m-7xa2/` 跳 303 到此。 |
| **P2 后台管理** | `/m-7xa2/` | `web/admin/index.html` + `app.js` + `style.css` | 登录后：①用户管理（列表/编辑/新增，可设 `fixed_provider`、`route_mode`、配额、到期、并发、单次上限）②调用统计（`cs-table` 全局多用户视图**含用户名列**；点用户弹 `calls-table` per-user 视图）③provider 管理（列表含「透传」列；表单含 `allow_passthrough` 勾选 / `auth_scheme` 下拉[bearer\|x-api-key\|none] / `auth_header` / `extra_headers`）。 |

### 1.2 后端功能模块

| 模块 | 源码位置 | 预期行为 / 输入-输出规范 | 错误码 |
|---|---|---|---|
| **A1** `/v1/chat/completions` 中转 | `proxy/handler.go` | SubKeyAuth → 并发上限 → 请求体预算(compaction) → 配额扣减 → 路由(fixed `GetProviderBySlug` / auto `ResolveProvider`) → 藏 Key → 上游转发 → 流式/同步 → 记账（`AddTokenUsage` sync+stream 两路均调，历史踩坑已修）→ 写 `call_logs` | 401/413/429/502/500 |
| **A2** `/v1/passthrough/<rest>` MCP 透传 | `proxy/passthrough.go` + `passthrough_request.go` | 通配子树、双开关（全局 `proxy.passthrough_enabled` + per-provider `allow_passthrough`，任一 false→403）、鉴权(401)、并发上限(429)、请求体预算(413)、上游目标构造(endpoint+subPath+query)、藏 Key、认证方案注入(bearer/x-api-key/none)、流式转发、调用日志(`Model="<METHOD> <subPath>"`)、上游不可达(502) | 401/403/413/429/502 |
| **A3** `/v1/calls` 用户自助 | `handler/calls.go` → `models.QueryCallLogs` | 按自己 `user_id` 查调用记录，分页（page/limit≤50） | 401 |
| **A4** `/v1/quota` 配额查询 | `handler/quota.go` → `models.QuotaStatus` | 返回 5h/总/Token 双维度 + `expires_at` + 累计 Token | 401 |
| **A5** `/admin/api/*` 后台 API | `admin/*.go` | 用户 CRUD、`UpdateUserRoute`、调用统计全局 `QueryCallLogsGlobal`+`AggregateCallStats`（已带 `username`）、provider CRUD（含透传字段） | 401/409/400/404 |
| **A6** 配额双维度 | `models/quota.go` | Token 总量（`limit=0`=无限）；调用次数（5h 窗口 + 累计总额，`limit=0`=锁死/无效，刻意不同）；倍率 `effectiveCalls=ceil(multiplier)`；Token 记账 `ceil(raw×mult)` | — |
| **A7** 路由优先级 | `router/selector.go` | auto 时间路由 `ResolveProvider` 按 `priority DESC → 窗口更窄优先 → id ASC`（overlapping 规则窄窗口覆盖宽窗口，不回退）；fixed `GetProviderBySlug` | 503 |
| **A8** 到期时间 `expires_at` | `models/expiry.go` + `handler/quota.go` | `QuotaStatus` 加 `ExpiresAt`；前端 `renderExpiryRow`；admin 列表同样渲染；非法/空格式 fail-closed | — |
| **A9** 配置与迁移 | `db/migrations.go` + `config/config.go` | DB 幂等迁移（`CREATE TABLE IF NOT EXISTS` + `columnExists` 守卫的 `ALTER`）；`config.yaml` 加载（含 `proxy.passthrough_enabled`、provider 透传字段） | — |
| **A0** `/v1/models` 模型发现 | `main.go` `ModelsHandler` | 公开 `GET`，无需鉴权 | — |
| **Auth** 鉴权中间件 | `auth/middleware.go` | 子 Key Bearer 鉴权 + 过期/禁用/删除检查；admin session 鉴权（页面 303 重定向 / API 401 JSON） | 401 `invalid_api_key` / 403 / 401 `not_authenticated` |
| **Security** 密钥加密 | `security/encrypt.go` | 上游 Key AES-256-GCM 加密落库；KEK 来自 `GATEWAY_KEK_ENV`；内存解密、绝不写日志 | — |

> 核对结论：地图所列模块在源码中**全部存在且行为与设计契约一致**。唯一与地图文字偏差：P1 地图写 `renderTokenRow`，实际 Token 行**内联**于 `renderDashboard`（无独立 `renderTokenRow` 函数），功能正确（见 §3 问题 #2，P3 级文档/命名偏差）。

---

## 2. 测试执行与结果

### 2.1 全量单测套件结果

**命令**（铁律：用 `/opt/homebrew/bin/go`，不用 make）：
```bash
export GATEWAY_KEK_ENV=test-kek-00000000000000000000000000000000
/opt/homebrew/bin/go test ./... -v
```

**结果：189 PASS / 0 FAIL，12 个包全部 `ok`，无编译错误，EXIT=0。**

| 包 | 结果 | PASS 用例数 | 关键覆盖点 |
|---|---|---:|---|
| `internal/admin` | ok (3.30s) | 18 | 用户 CRUD、并发上限(0/负/超限 400)、count 限额 0 拒绝 400、expiry 规范化、provider 透传字段 |
| `internal/auth` | ok (3.48s) | 19 | 子 Key 鉴权(缺失/非法/过期/禁用/删除)、fail-closed 过期、admin 免过期、session 鉴权(重定向/JSON) |
| `internal/config` | ok (3.47s) | 4 | 加载默认值 / 环境变量覆盖 / 缺文件 / 示例文件 |
| `internal/db` | ok (3.72s) | 6 | 建表、**迁移幂等**、默认路由规则种子、+4 列、Token 用量回填、唯一约束 |
| `internal/handler` | ok (3.85s) | 7 | `/v1/quota`：count=0 不崩、expires_at 场景/JSON 契约、Token 超限 remaining 钳制 |
| `internal/models` | ok (4.22s) | 34 | 双维度扣减(5h/总/Token 软门限/0 语义)、`QueryCallLogsGlobal` 带 username、模型过滤大小写不敏感、聚合 by_model |
| `internal/provider` | ok (4.21s) | 11 | provider CRUD、透传字段、seed、routing rule、audit |
| `internal/proxy` | ok (5.24s) | 52 | chat handler 全回归(配额/并发/流式 Token 记账/compact)、**passthrough 全 22+ 用例**(藏 Key/双开关 OFF/配额/413/502/流式/未知 method 透传/Model 约定) |
| `internal/quota` | ok (1.65s) | 7 | `CheckAndDeduct`、倍率引擎(时区/跨夜窗口) |
| `internal/router` | ok (1.57s) | 20 | `ResolveProvider`(重叠规则窄窗口胜/优先级胜/不回退)、`RewriteModel` 大小写、时区锁定 |
| `internal/security` | ok (1.61s) | 6 | KEK 派生、AES-256-GCM 加解密往返、错 Key/损坏数据失败、MaskKey |
| `internal/timeutil` | ok (1.53s) | 5 | 上海时区 UTC+8、`IsInRange`(普通/跨夜/退化端点)、`MatchDay` |
| **合计** | **12/12 ok** | **189** | — |

### 2.2 页面可达性与结构检查（curl + grep）

| 检查项 | 命令/方法 | 期望 | 实际 | 结果 |
|---|---|---|---|---|
| `/user/` 可达 | `curl -o /dev/null -w "%{http_code}" /user/` | 200 | **200** | ✅ |
| `/m-7xa2/` 登录重定向 | `curl -w "%{http_code} %{redirect_url}"` | 303 → login | **303 → /m-7xa2/login** | ✅ |
| `/m-7xa2/login` 可达 | `curl -w "%{http_code}"` | 200 | **200** | ✅ |
| P1 关键函数存在 | grep `web/user/index.html` | `renderCountRow`/`renderExpiryRow` | 均存在（第 346 / 366 行）；Token 行内联于 `renderDashboard`（见 §3 #2） | ✅ |
| P2「用户名」列 | grep `web/admin/index.html:231` (`cs-table` thead) | `<th>用户名</th>` | **存在**（ID/用户名/模型/Token/消耗次数/倍率/状态码/延迟/时间/上游） | ✅ |
| P2「透传」列 | grep `web/admin/index.html:106` (providers thead) | `<th>透传</th>` | **存在** | ✅ |
| P2 透传开关表单 | grep `web/admin/index.html:271-273` | 通配透传/MCP + `allow-passthrough` 勾选 | **存在**；`app.js` 读写 4 字段（allow_passthrough/auth_header/auth_scheme/extra_headers） | ✅ |

### 2.3 线上 MCP 透传正常流（3 子路径 `initialize`）

网关基址 `https://ai.shimiaocheng.top`，上游基址 `https://open.bigmodel.cn/api/mcp`。每个子路径 `POST initialize`（`Accept: application/json, text/event-stream`）。

| 子路径 | 期望 | 实际 | 结果 |
|---|---|---|---|
| `/v1/passthrough/web_search_prime/mcp` | 200 + 合法 serverInfo | 200，`serverInfo.name=mcp-web-search-prime v0.0.1` | ✅ |
| `/v1/passthrough/web_reader/mcp` | 200 + 合法 serverInfo | 200，`serverInfo.name=web-reader-server v0.0.1` | ✅ |
| `/v1/passthrough/zread/mcp` | 200 + 合法 serverInfo | 200，`serverInfo.name=zread-server v0.0.1` | ✅ |

> 三者均返回合法 `serverInfo` → 证明：通配路由命中、全局开关开、子 Key 鉴权通过、per-provider 闸门开、上游目标拼接正确、真实 Key 注入成功、流式 SSE 转发正常。

### 2.4 线上 MCP 透传边界 / 异常

| 场景 | 类型 | 步骤 | 期望 | 实际 | 结果 |
|---|---|---|---|---|---|
| 无 Authorization 头 | exception | `POST` 不带鉴权头 | 401 | **401** `{"error":{"type":"invalid_api_key",...}}` | ✅(注1) |
| 未知 JSON-RPC method | exception | `method=this_method_does_not_exist_anywhere` | 上游错误原样透传 | 200，`{"error":{"code":-32601,"message":"Method not found: ..."}}` 原样 | ✅ |

> 注1：401 功能正确（拒绝），但 `type` 字面量为 `invalid_api_key`，与文档「`not_authenticated`」不符（见 §3 问题 #1，P3）。
> 空子路径 / 超长子路径 / GET 无 Accept 等边界，今日早些时候的 PR #17 专项报告（同 `docs/qa-mcp-passthrough-2026-07-19.md`）已实测通过；本次复测 3 正常 + 无鉴权 + 未知 method 全部通过，边界行为稳定。

### 2.5 配额双维度 + 调用日志实证（SSH 只读 DB）

- **双维度语义**：`quotas` 表含 `quota_token_total_limit`/`quota_token_total_used`（Token 维度，`0`=无限）与 `quota_5h_limit/quota_total_limit`（次数维度，`0`=锁死）。线上 16 个用户 `token_total_limit=0`（无限），无 `count` 限额=0 的遗留锁死行 → 双维度「刻意不同」语义成立。
- **配额扣减实证**（对 `mcp` 用户，route_mode=fixed→zhipu-mcp）：

| 指标 | 额外 1 次透传前 | 额外 1 次透传后 | 结论 |
|---|---:|---:|---|
| `quota_total_used`（次数维度） | 34 | **35** | ✅ +1（每次 `effective_calls=ceil(1.0)=1`） |
| `quota_token_total_used` | 0 | 0 | 透传路径不调 `AddTokenUsage`（仅记次数，符合 A6 设计）；`mcp` 用户 `token_total_limit=0` 无限 |
| 新增 `call_logs` 行 | — | `id=10141` `Model='POST /web_search_prime/mcp'` `provider_id='zhipu-mcp'` `status=200` `effective_calls=1` `multiplier=1.0` | ✅ M15 集成生效 |

- `mcp` 用户配额行：`(username='mcp', 5h_used=5, 5h_limit=100, total_used=35, total_limit=1000, token_total_limit=0, token_total_used=0)`。
- 全库 passthrough 风格 `call_logs`（`Model LIKE 'POST /%' OR 'GET /%'`）共 **35** 行。

### 2.6 双开关负路径（线上已开，由单测覆盖 OFF）

- ✅ 线上全局开关：`SSH grep config.yaml` → 第 35 行 `passthrough_enabled: true`。
- ✅ 线上 Provider 闸门：`SSH SELECT` → `zhipu-mcp.allow_passthrough = 1`、`auth_scheme=bearer`、`auth_header=Authorization`、`endpoint=https://open.bigmodel.cn/api/mcp`、`enabled=1`。
- ✅ 开关 **OFF** 负路径由既有单测覆盖（不可线上 toggle）：`TestPassthrough_GlobalSwitchOff` → 全局关 → 403；`TestPassthrough_ProviderDisabled` → `allow_passthrough=false` → 403。

### 2.7 综合用例结果表（线上 + DB）

| 模块 | 场景 | 类型 | 步骤 | 期望 | 实际 | 结果 |
|---|---|---|---|---|---|---|
| P1 | 页面可达 | normal | `GET /user/` | 200 | 200 | ✅ |
| P2 | 登录重定向 | normal | `GET /m-7xa2/` | 303→login | 303→login | ✅ |
| P2 | 用户名列/透传列 | normal | grep HTML | 含列 | 含列 | ✅ |
| A2 | 3 子路径 initialize | normal | POST initialize | 200+serverInfo | 200+serverInfo×3 | ✅ |
| A2/M3 | 无鉴权 | exception | 无 Authorization | 401 | 401 | ✅(注1) |
| A2/M14 | 未知 method | exception | 错误 method | 原样透传 | -32601 原样 | ✅ |
| A2/M10/M11 | 藏 Key | normal | 上游返业务数据 | 真实 Key 注入 | 已证实(serverInfo) | ✅ |
| A6/M15 | 配额扣减+日志 | normal | 1 次透传 | total_used+1、写日志 | 34→35、id=10141 | ✅ |
| A9 | 迁移幂等 | normal | 重跑迁移 | 不报错 | 索引存在+单测覆盖 | ✅ |

---

## 3. 问题清单（含严重度与证据）

| # | 严重度 | 模块 | 问题描述 | 触发条件 | 证据 | 复现/验证 | 建议 |
|---|---|---|---|---|---|---|---|
| **1** | **P3 轻微** | A2/M3、Auth | 文档约定无/非法子 Key → `401 not_authenticated`，**实际返回 `invalid_api_key`**（功能正确，错误 `type` 字面量不符） | 不带/带非法 `Authorization` 头 | 线上：`{"error":{"type":"invalid_api_key"}}`；源码 `auth/middleware.go:52/58/71` `writeAuthError(...,"invalid_api_key")`；既有单测 `middleware_test.go` 亦断言 `invalid_api_key` | `POST /v1/passthrough/...` 不带 Authorization → 401 | 二选一：①更新文档以匹配共享中间件实际行为；②若需统一为 `not_authenticated`，改 `middleware.go`（影响所有端点，需评估） |
| **2** | **P3 轻微** | P1（文档/命名） | 功能地图写 P1 含 `renderTokenRow`，**实际代码将 Token 总量行内联于 `renderDashboard`**（无独立 `renderTokenRow` 函数）；功能正确（`limit=0`→「无限」） | 代码核对 | `web/user/index.html:404-420`（内联 tokenRowHtml）、`:346` renderCountRow、`:366` renderExpiryRow | grep 源码确认 | 地图文档与实际对齐即可，非缺陷 |

> **结论：未发现 P0/P1/P2 级阻断或严重缺陷。** 仅 2 项 P3 级（其一为已知历史问题 #1，共享鉴权中间件行为，非 PR #17 引入；其二是文档/命名偏差，不影响功能）。

---

## 4. 依赖与集成检查结论

| 检查项 | 方法 | 结论 |
|---|---|---|
| `/v1/passthrough/` 与 `/v1/chat/completions` 独立 | 源码 `main.go:201,209` | ✅ 两路由独立 `mux.Handle`，分别绑定 `proxyHandler`（chat）与 `passthroughHandler`（透传），仅共享 `SubKeyAuth`/`quota`；`recoverMiddleware` 按请求级别捕获 panic，互不干扰 |
| 各表结构字段齐全 | SSH `PRAGMA table_info` | ✅ users 含 `expires_at/route_mode/fixed_provider/max_body_size/max_concurrency`；quotas 含 `fixed_multiplier/quota_token_total_used/quota_token_total_limit`；providers 含 `allow_passthrough/auth_header/auth_scheme/extra_headers`；call_logs 含 `provider_id` |
| 外键引用一致 | 源码 + PRAGMA | ✅ `quotas.user_id`/`call_logs.user_id` → `users(id) ON DELETE CASCADE`；`model_mappings.provider_id` → `providers(slug)`；`DeleteProvider` 校验 routing_rules/mappings/fixed_provider 引用，避免悬空 |
| `zhipu-mcp` 配置正确 | SSH `SELECT` | ✅ `endpoint=https://open.bigmodel.cn/api/mcp`（base URL，未拼 /chat/completions，符合设计 Q1 隔离）、`allow_passthrough=1`、`auth_scheme=bearer`、`auth_header=Authorization`、`enabled=1` |
| 调用日志集成（M15） | SSH `SELECT call_logs` | ✅ 生产数据 `Model='POST /web_search_prime/mcp'`、`provider_id='zhipu-mcp'`、`status_code=200`、`effective_calls=1`、`multiplier_used=1.0`；全库 35 行透传风格日志 |
| 配额扣减（A6） | SSH 前后对比 | ✅ 单次成功透传 `quota_total_used` 34→35（+1）；`mcp` 用户 `max_body_size=1048576`、`max_concurrency=10` |
| 前端字段落点（M18/A8/P2） | 前端静态 grep | ✅ `cs-table` 含「用户名」列；providers 表含「透传」列；provider 表单含 `allow_passthrough`/`auth_header`/`auth_scheme`/`extra_headers`；`renderExpiryRow`（P1）与 admin 列表到期渲染一致；`/v1/quota` 返回 `expires_at` 与前端字段名一致 |
| DB 迁移幂等（A9） | 单测 + PRAGMA | ✅ `TestRunMigrations_Idempotent` PASS；线上索引 `idx_call_logs_provider_created`/`idx_users_sub_key_hash` 等均存在；`migrations.go` 全部用 `IF NOT EXISTS` + `columnExists` 守卫 `ALTER`，可安全重跑 |
| 服务状态 | SSH `systemctl is-active` | ✅ `active`（仅读状态，未 restart） |
| 密钥加密（Security） | 单测 + 源码 | ✅ `TestEncryptDecrypt_RoundTrip`/`TestDecrypt_WrongKey`/`TestDecrypt_CorruptedData` 全 PASS；KEK 来自 `GATEWAY_KEK_ENV`，密钥内存解密、绝不写日志（ADR-0002/0007） |

---

## 5. 总体结论与建议（含未覆盖项）

### 结论：**PASS（建议通过）**

- **单测套件**：全量 `go test ./...` → **12 包 189 PASS / 0 FAIL**，覆盖 A1–A9、Auth、Routing、Security、Config、DB 迁移、双维度配额、调用统计全局 username、MCP 透传全路径。
- **页面**：`/user/` 200、`/m-7xa2/` 303→login 200；P1 `renderCountRow`/`renderExpiryRow` 存在，Token 行内联（功能正确）；P2「用户名」列 + 「透传」列 + 透传开关表单齐全。
- **线上 MCP 透传**：3 子路径 `initialize` 全部 200 + 合法 `serverInfo`；无鉴权→401；未知 method 原样透传；藏 Key 经「上游返回真实业务数据」实证成立。
- **配额双维度 + 集成**：`quota_total_used` 前后对比 34→35（+1）实证；新增 `call_logs`（`Model='POST /web_search_prime/mcp'`）实证 M15；双维度语义（次数 `limit=0` 锁死 / Token `limit=0` 无限）成立。
- **集成检查**：双路由独立、外键一致、`zhipu-mcp` 配置正确、迁移幂等、服务 active，全部通过。

### 问题
- 仅 2 项 **P3**：①M3 错误 `type` 字面量偏差（已知历史问题，共享中间件行为）；②地图 `renderTokenRow` 命名与实际内联实现不符（非缺陷）。均不影响安全与功能正确性。

### 建议
1. 统一 M3 错误 `type` 文档或中间件行为（二选一，评估影响面）。
2. 地图文档将 P1 的 `renderTokenRow` 改为「内联于 `renderDashboard`」，避免误导。

### 未覆盖项与限制说明（已标注替代验证）
1. **后台内部 API 业务逻辑（A5 CRUD/查询）**：无 admin 密码/token，无法深度测。替代：①curl 测 `/m-7xa2/` 返回 303（登录页可达，已证）；②前端静态 grep 确认表单/列/字段落点（已证）；③SSH 只读查 DB 验证底层数据模型（users/providers/quotas/call_logs 字段齐全、外键一致，已证）；④相关逻辑由 `internal/admin` 18 个单测覆盖（用户 CRUD、并发/限额校验、expiry 规范化、provider 透传字段）。
2. **chat 端点线上验证（A1）**：无 chat-pinned 子 Key，无法线上打 `/v1/chat/completions`。替代：①`internal/proxy` 52 个单测含 chat handler 全回归（配额/并发/流式 Token 两路记账/compact/路由）；②源码确认两 Handler 独立 `mux.Handle`；③调用统计全局 username 由 `TestQueryCallLogsGlobal_UsernamePopulated` 覆盖。
3. **双开关 OFF 负路径**：线上全局开 + Provider 开，无法直接 toggle。已由 `TestPassthrough_GlobalSwitchOff` / `TestPassthrough_ProviderDisabled` 覆盖。
4. **并发上限 429 / 配额耗尽 429 / 上游不可达 502**：线上难稳定触发。已分别由 `TestPassthrough_ConcurrencyLimit` / `TestPassthrough_QuotaExceeded`(+`TestPassthrough_CallLogModelConvention`) / `TestPassthrough_UpstreamUnreachable` 覆盖；配额**扣减**已线上实证（§2.5）。
5. **>32MB 超大 body 的 32MB clamp 边界**：2MB 已实测触发 413；32MB 上限 clamp 由源码 `passthrough.go:98-100`（clamp 到 `MaxBodySizeCeiling`）保证，建议后续补 `maxBodySize=32MB+1` 边界单测（既有 `TestPassthrough_BodyLimitExceeded` 验证 budget 级 413）。
6. **透传路径不累加 Token 用量**：MCP 透传仅扣「调用次数」、不调 `AddTokenUsage`，故 `quota_token_total_used` 对透传用户不增长（设计上透传为 call-count 模型；且 `mcp` 用户 `token_total_limit=0` 无限）。属已知设计边界，供运维知悉：MCP 用户的「累计 Token」统计不反映透传流量。

> 报告生成完毕。所有「通过」结论均附执行证据（单测用例名 / curl 输出片段 / SSH 查询结果）。受限项已明确标注替代验证方式。
