# LLM API 网关 — D1 修复验收 + 回归冒烟报告

**QA：严过关（Edward）**　|　**日期**：2026-07-14（Asia/Shanghai）
**修复缺陷**：D1 — `internal/models/call_stats.go` 的 `AggregateCallStats` 给两个 `CASE SUM` 补 `COALESCE(...,0)`，新增 `call_stats_test.go` 空态回归测试

---

## 1. 构建 arch 确认
- 命令：`CGO_ENABLED=0 GOOS=linux GOARCH=amd64 /tmp/go/bin/go build -o /tmp/llm_api_gateway_linux .`（Go go1.22.10 darwin/arm64 交叉编译到 linux/amd64）
- 本地产物 `file`：**`ELF 64-bit LSB executable, x86-64, statically linked`** ✅（避免 arm64 Exec format error）
- 服务器落盘后 `file` 复核：**`ELF 64-bit LSB executable, x86-64`** ✅

## 2. 部署结果（标准回滚）
| 项 | 结果 |
|---|---|
| 旧二进制备份 | `/opt/llm-gateway/llm_api_gateway.bak.1783992880`（15,955,419 B），可回滚 ✅ |
| `systemctl stop` | rc=0 ✅ |
| `scp` + `chown llm-gw:llm-gw` + `chmod 755` | ✅ 权限 `-rwxr-xr-x llm-gw llm-gw` |
| `systemctl start` → `is-active` | **active** ✅ |
| 端口 8080 监听 | `127.0.0.1:8080 LISTEN`（pid 397720）✅ |
| TZ 生效确认 | 监听进程 environ 含 **`TZ=Asia/Shanghai`** ✅（上轮已加 Environment，本轮未改 unit）|
| 启动日志 | 无 panic/error，migrations 完成，Admin routes registered ✅ |

## 3. D1 验收：空数据态 stats（核心修复点）
- 请求：`GET /m-7xa2/api/calls/stats?model=__nonexistent__`
- **实际 HTTP 码：200** ✅（修复前为 500）
- **响应体（全 0）**：
```json
{"total_calls":0,"tokens":{"prompt":0,"completion":0,"total":0},"effective_calls":0,"success":{"success_count":0,"error_count":0,"success_rate":0}}
```
✅ 与期望完全一致，**D1 验收通过**。

## 4. 快速回归（无回归引入）
| 端点 | HTTP | 关键字段 |
|---|---|---|
| R1 全量 `stats` | 200 | total_calls=**608**, tokens.total=**69732072**, effective=**1024**, success=**595**, error=**13**（与上次一致量级）✅ |
| R2 `calls?provider_id=zhipu&近7天` | 200 | 结构 `{data, pagination}`，data_len=20，pagination `{page:1,limit:20,total:423}` ✅ |
| R3 `calls/models` | 200 | `["GLM-5.2","astron-code-latest","glm-5.2"]` ✅ |
| R4 组合筛选 `calls?model=glm-5.2&近7天` | 200 | data 内 model 全为 `glm-5.2`，total=136，筛选生效 ✅ |

## 5. 冒烟测试
| 项 | HTTP | 结果 |
|---|---|---|
| login（`/m-7xa2/api/login`，admin） | 200 | `{"message":"Login successful"}`，cookie 已存 ✅ |
| `/v1/chat/completions`（glm-5.2，Bearer subkey） | 200 | **model 回写 `glm-5.2`** ✅ |
| 重名 409（`POST /m-7xa2/api/users` username=admin） | 409 | `{"error":"username already exists"}` ✅ |
| 临时 QA 用户清理 | 200 | `DELETE /m-7xa2/api/users/38` → `User deleted`；随后用其 subkey 调 chat 返回 401，确认无残留 ✅ |

> 冒烟说明：chat 端点在 nginx 的 **`/v1/`** 公开 location（Bearer subkey 鉴权），**不是** `/m-7xa2/`（该前缀映射到网关 `/admin/`，需 admin session）。首次误用 `/m-7xa2/v1/...` 被 nginx 重定向到登录页（303），改用正确 `/v1/chat/completions` 后 200 + model 回写正常。

## 6. 智能路由判定
# **NoOne** —— 全部通过，无需回派 Engineer。

## 7. 遗留风险 / 注意
1. **路径约定易误用（运维层面，非代码缺陷）**：外部调用 chat/completions、/v1/quota、/v1/calls 必须走 `https://ai.shimiaocheng.top/v1/...`（Bearer subkey）；`/m-7xa2/...` 仅对应管理面 `/admin/`（session 鉴权）。建议在接口文档/客户端示例中明确区分，避免再踩 303 坑。
2. **minimal 调用 content 为空**：glm-5.2 在极简 prompt（1 词、max_tokens=16）下 `choices[0].message.content` 返回空串，但 `model=glm-5.2`、HTTP 200 均正确，路由与鉴权无误；如业务对空 content 敏感可后续观察（非本次 D1 范畴）。
3. **回归数值随真实调用微变**：total_calls/tokens 等随线上新请求自然增长，本轮 608 / 69732072 与上次一致量级，结构正确，无需告警。
4. 备份链健康：本轮备份 `bak.1783992880` 保留，回滚路径可用。

---
**结论**：D1 已修复并验证（空态 stats 由 500 → 200 全 0），构建/部署/回滚规范，回归与冒烟全绿，**判定 NoOne，可上线**。
