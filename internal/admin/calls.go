package admin

// This file is intentionally minimal — the calls.go functionality
// (GetUserCalls) is implemented in users.go as it's tightly coupled
// to the user resource.
//
// The GET /admin/api/users/{id}/calls endpoint is handled by
// Handler.GetUserCalls in users.go.
